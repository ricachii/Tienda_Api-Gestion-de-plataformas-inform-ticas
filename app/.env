# JWT Configuration
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRE_MIN=60
# Nota: Reemplace los valores de ejemplo con sus credenciales reales.
# Editar app/.env (haz backup y cambia DB_HOST)
# Reemplaza o añade la línea DB_HOST con la IP de VM2
# Verifica contenido

DB_HOST=192.168.56.20
DB_PORT=3306
DB_USER=tienda_user
DB_PASS=Lulita2014
DB_NAME=tienda
